# Ubuntu-specific packages only (connection settings handled by constructed.yaml)
common_packages:
  - curl
  - wget
  - vim
  - htop
  - net-tools
  - open-vm-tools

# Repository updates
update_cache: yes
upgrade_packages: safe