plugin: aws_ec2
aws_profile: brine
regions:
  - us-west-2

keyed_groups:
  - key: tags
    prefix: tag
    separator: "_"

filters:
  instance-state-name: running

hostnames:
  - ip-address  

compose:
 ansible_host: ip-address
